# NODEJS TYPEORM SCAFFOLD BACKEND

## STEPS

### Step 1
Clone the repo

### Step 2
Run `npm install`

### Step 3
Start your docker desktop if availbale or start docker

`docker compose build`

`docker compose up`

The backend will be deployed at `127.0.0.1:4000`